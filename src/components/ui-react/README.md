# UI React Islands Conventions

- Only place React components under `src/components/ui-react/**`.
- Default hydration strategy is `client:visible`.
- Do not import React islands at docs/global layout level.
- Use `src/pages/ui.astro` as the single showcase page for interactive demos.
